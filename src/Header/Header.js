import React from 'react';
import './Header.css';
import BootstrapNav from './BootstapNav';

const Header = () => {
    return (
        <header>
            <BootstrapNav />
        </header>
    );
};

export default Header;